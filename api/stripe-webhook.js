/**
 * Stripe webhook handler — Phase 4
 * Placeholder only. Do not implement until Phase 4.
 */
export default function handler(req, res) {
  res.status(503).json({ error: 'Payments available in Phase 4.' })
}
